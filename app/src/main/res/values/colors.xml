<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Matches the game's `bg` token so the adaptive icon's masked area reads
         as the same deep space the app itself sits on. -->
    <color name="ic_launcher_background">#0B1622</color>
    <!-- The window background painted before Compose draws its first frame.
         Left at the system default it flashes white into a dark game. -->
    <color name="splash_background">#0B1622</color>
</resources>
