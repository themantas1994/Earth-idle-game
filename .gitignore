node_modules/
dist/
dist-ssr/
*.local
.DS_Store
*.log
coverage/
.vite/
*.tsbuildinfo

# --- Android ---
# Build outputs and machine-local SDK paths are regenerated by
# `npm run android:build`; nothing under them belongs in the repo.
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/capacitor-cordova-android-plugins/build/
android/app/release/
android/app/debug/
.gradle/

# Signing material must never be committed — see README "Signing a release".
*.jks
*.keystore
keystore.properties

# --- Native Android (Gradle) ---
/build/
/app/build/
/.gradle/
/local.properties
/.idea/
/captures/
