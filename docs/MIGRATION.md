# TypeScript → Kotlin migration map

The record of how the original web game became a native Android app: what each
piece of the old code became, what was verified and how, and what deliberately
differs.

## Where everything went

| TypeScript | Kotlin | Notes |
| --- | --- | --- |
| `engine/bignum.ts` | `domain/engine/GameDecimal.kt` | Same sign/mantissa/exponent representation, same significance cutoff, same log-space `pow`. Kotlin has no operator overloading by convention name, so `add`/`mul` became `+`/`*`. |
| `engine/format.ts` | `domain/formatting/NumberFormatting.kt` | All four notations. Needed a `jsToFixed` helper: ECMAScript's `toFixed` rounds the exact binary value half-away-from-zero, and `toLocaleString` rounds the *shortest round-tripping decimal* — two different rules that a naive `String.format` gets wrong in opposite directions. |
| `engine/constants.ts` | `domain/engine/Constants.kt` | Values unchanged, as objects rather than exported consts. |
| `engine/gases.ts` | `domain/model/Gases.kt` | `GasId` became an enum, so per-gas containers can be flat arrays indexed by ordinal instead of hash maps — the tick touches every gas of every generator. |
| `engine/resources.ts` | `domain/model/Resources.kt` | Likewise for `ResourceId`. |
| *(new)* | `domain/model/Amounts.kt` | `GasAmounts` / `GasDoubles` / `ResourceAmounts`: immutable array-backed containers with mutable builders, replacing `Record<GasId, Decimal>`. |
| `engine/climate.ts` | `domain/climate/Climate.kt` | Analytic integration preserved exactly, including its step-size independence. |
| `engine/habitability.ts` | `domain/climate/Habitability.kt` | Five factors, still multiplied. |
| `engine/ownership.ts` | `domain/engine/Ownership.kt` | |
| `engine/simulation.ts` | `domain/engine/Simulation.kt` | `computeProductionRates` accumulates into builders rather than materialising a per-technology result — same numbers, far fewer allocations per tick. |
| `engine/economy.ts` | `domain/economy/Economy.kt` | The price-stability invariant and its rationale carried over verbatim. |
| `engine/offline.ts` | `domain/engine/Offline.kt` | |
| `engine/technologies/*.ts` | `domain/technologies/*Technologies.kt` | All 99, translated by a parser rather than by hand, then checked field-by-field against the reference. |
| `engine/technologies/scaling.ts` | `domain/technologies/Scaling.kt` | |
| `engine/technologies/index.ts` | `domain/technologies/TechnologyRegistry.kt` | Plus precomputed choice-group membership and the two disjoint screen lists. |
| `engine/prestige.ts` | `domain/prestige/Prestige.kt` | |
| `engine/achievements.ts` | `domain/achievements/Achievements.kt` | |
| `engine/challenges.ts` | `domain/challenges/Challenges.kt` | |
| `engine/events.ts` | `domain/events/Events.kt` | |
| `engine/milestones.ts` | `domain/milestones/Milestones.kt` | |
| `engine/gameState.ts` | `domain/model/GameState.kt` | |
| `engine/save.ts` | `domain/save/SaveSerialization.kt` + `Migrations.kt` | Same wire format, so a web save still loads. Storage moved behind `SaveRepository`. |
| `store/useGameStore.ts` | `domain/engine/GameLoop.kt` + `presentation/GameViewModel.kt` | Split deliberately: the session *rules* (absences, event rolls, milestone firing, challenge settlement) went to the domain layer where they are testable without Android; only the *plumbing* (coroutines, lifecycle, StateFlow, autosave) stayed in the ViewModel. |
| `ui/screens/*.tsx` | `presentation/screens/*.kt` | One Compose screen per React screen, same information architecture. |
| `ui/components/*.tsx` | `presentation/components/*.kt` | |
| `styles/global.css` | `presentation/theme/Theme.kt` | The CSS custom properties became a `GameColors` object provided through a CompositionLocal; the era tinting became `VisualEra`. |
| `platform/native.ts` | `platform/haptics/`, `MainActivity` | Capacitor Haptics → `VibrationEffect`; `appStateChange` → `ProcessLifecycleOwner`; hardware back → `BackHandler` over an explicit destination stack. |
| `platform/ads.ts` | `platform/ads/Monetization.kt` + `presentation/components/BannerAd.kt` | Rewritten against the Mobile Ads SDK and UMP directly. The banner is a real view in the layout now, so the CSS-inset dance the WebView needed is gone. |
| `scripts/exportParityFixtures.ts` | `tools/ts-reference/exportParityFixtures.ts` | Kept, as the fixture generator. |
| React, React DOM, Vite, Zustand, Capacitor (core, app, haptics, preferences, splash-screen, status-bar), `@capacitor-community/admob`, jsdom, Playwright | *removed* | |

## How the port was verified

The reference engine was run and its answers captured as JSON, and the Kotlin
suite asserts against those rather than against a re-reading of the source. The
fixtures live in `app/src/test/resources/parity/` and are regenerated by
`tools/ts-reference`.

| Fixture | What it pins down |
| --- | --- |
| `decimal.json` | 24 operands from 1e-400 to 1e1000: every unary op, all 576 binary pairs, 104 `pow` cases, and the save-triple round trip. |
| `format.json` | 36 values × 4 notations × 2 precisions, plus durations, temperatures and percentages — compared as strings. |
| `technologies.json` | All 99 technologies, every field, plus the tier curves at 45 tiers. |
| `economy.json` | Quoted and bulk prices, buy-max affordability including the exact geometric-series boundary, the prestige discount, and the ownership ladder. |
| `climate.json` | 3,240 gas integrations across every gas, production rate, removal state, sink efficiency and step size from 250 ms to a day; forcing, temperature, ocean chemistry and all five habitability factors. |
| `prestige.json` | The upgrade table and cost ladders, nine ownership combinations including everything maxed, the speed term, and payouts for gas totals from zero to 1e120. |
| `content.json` | The achievement, challenge, event, milestone, gas and resource tables — including each challenge's *computed* technology lockout set. |
| `events.json` | Every eligibility state × draw, so a seeded roll picks the same event. |
| `simulation.json` | A 95-step scripted playthrough from a fresh game to collapse at +600,000 °C and through a prestige reset, comparing every simulation value at every step. |
| `offline.json` | Absences from 0 to a week, with and without the cap upgrades, enabled and disabled. |
| `save.json` | Saves the reference actually wrote, plus the v1→v2→v3 migration results. |

**Tolerances** are documented in `parity/Fixtures.kt`. Values that are copies
rather than computations must match exactly; a single arithmetic operation is
held to 1e-9 relative; the end of a long dependent chain to 1e-6. The two
engines run identical formulas but through different `pow`/`log10`/`exp`
implementations, which are permitted to differ in the last ulp, so error
accumulates slightly differently across thousands of dependent operations.
Decimal comparisons are made in log space, so a value near 1e1000 is held to the
same *relative* accuracy as one near 1.

## Deliberate differences

Two, both narrow, both documented where they live.

**The Speedrun challenge** used `Date.now() - state.runStartedAt` inside its
goal check, which made a pure predicate depend on a wall clock: the same state
could give different answers on consecutive calls, and it could not be tested at
all. It now measures from `state.lastTickAt`, which the tick sets to the current
time on every step. Behaviour in a live game is identical to within one tick.

**Water vapour lags one simulation step.** It is a feedback driven by the
*previous* step's temperature rather than an accumulating stock, so unlike every
other gas it is not step-size independent: one 3600-second call sees a
temperature of zero throughout where 14,400 quarter-second calls do not. This
matches the reference exactly — its own step-size test likewise checks only the
accumulating quantities — and nothing the player spends or is scored on depends
on it. H₂O emits nothing, is never banked, and contributes forcing only through
a temperature the other gases already set.

Everything else — every formula, constant, threshold, price, growth rate,
technology, achievement, challenge, event and headline — is unchanged.

## What is not proven by execution

The build environment has no KVM, so no emulator can boot and no physical device
is attached. To close as much of that gap as possible the Compose UI, the
DataStore save repository and a headless end-to-end playthrough all run under
Robolectric as part of `./gradlew test`, rather than being left to an
instrumented suite nobody ran.

What remains unverified by execution is the platform layer proper: the vibrator,
`SoundPool`, and the Mobile Ads SDK and its consent form. Those are structurally
correct and every failure path is defensive, but they have not been run against
real hardware. Installing the debug APK closes it.
